rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // MVP: allow all reads/writes for authenticated users.
    // TODO: tighten per-collection rules before production launch.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
