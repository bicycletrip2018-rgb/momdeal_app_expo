# SAVEROO_ALGORITHM_SPEC.md — 세그먼트 매칭 & 데이터 아키텍처

> SAVEROO_TECH_SPEC_v2026.06.md §12(FILE REFERENCE MAP)에서 참조만 되고 실체가 없던 문서. 이 문서가 그 실체이며, "유사 세그먼트 매칭"을 중심으로 한 데이터 모델·알고리즘의 SSOT다.
> 최초 작성: 2026-08-12 (Claude와의 기획 세션 기준). 이후 결정 변경 시 이 문서를 직접 갱신한다 — 사람이 md를 직접 편집할 필요 없음.

---

## 0. 이 문서의 위치

세이브루는 "맘카페(투명한 리뷰/정보 공유) + 폴센트류 가격추적"을 결합한 앱이며, 쿠팡 파트너스로 수익화한다. **경쟁사 대비 가장 큰 차별점은 "익명 다수"가 아니라 "내 아이와 유사한 조건의 부모" 리뷰/관심상품을 보여주는 세그먼트 매칭 엔진**이다. 이 문서는 그 엔진의 근거·데이터모델·알고리즘을 정의한다.

### 0.1 이 엔진이 뒷받침하는 4대 전략 축

단순 가격추적 앱과의 차별점은 세그먼트 매칭 엔진 하나로 아래 4가지가 동시에 성립한다는 점이다. §4~§6의 모든 축 설계 판단(무엇을 묻고, 무엇을 안 묻고, 언제 배제하는지)은 이 4가지 중 어디에 기여하는지로 검증한다.

| 전략 축 | 이 문서에서의 근거 |
|---|---|
| ① 뾰족한 사용 이유(솔직한 또래 후기 + 가격추적 + 절약) | §3 리뷰신뢰도체계(구매인증 우선노출) + §4 세그먼트 매칭이 "내 아이와 유사한 조건" 후기를 골라 보여줌 |
| ② 유저 데이터 명확성 → 향후 비즈니스 확장 | §5 미래서비스확장원칙 전체(특히 §5.1 `interestAreas` 점진적 프로파일링) — 지금 다 묻지 않고도 나중에 정확히 타겟할 수 있는 구조 |
| ③ 정밀 마케팅 타겟팅 | §5.2 — 신규 동의 인프라 없이 `interestAreas` + 세그먼트 태그 조합만으로 채널/서비스별 타겟 발송 가능 |
| ④ 커뮤니티 기반 MAU 확보 | §3 신뢰도 체계가 "인증 후기만 보기" 등으로 콘텐츠 품질을 유지해야 재방문이 발생 — 세그먼트 매칭이 없으면 커뮤니티는 "익명 다수"로 퇴화해 ①과 ④가 동시에 무너짐 |

축을 추가할지 말지 판단할 때(§4, §5.4)도 이 표가 기준이다 — 위 4가지 중 어느 것도 강화하지 못하면서 축 개수만 늘리는 질문(예: 소득구간, 상세 건강기록)은 §5.5에서 원칙적으로 배제한다.

---

## 1. 아이 발달 단계 프레임워크 (WHO 근거)

기존 6단계는 근거가 불명확했다. 아래는 각 경계값을 WHO 공식 정의/가이드라인에 정확히 맞춘 8단계 + 임신기 체계다.

| stage 값 | 연령 구간 | 과학적 근거 |
|---|---|---|
| `pregnancy` | 임신 전체 (1/2/3분기: 1~13주 / 14~27주 / 28주~) | WHO 산전관리(ANC) 모델의 3분기 구분 |
| `newborn` | 생후 0~28일 | WHO의 신생아기(neonatal period) 공식 정의 = 출생 후 28일 |
| `early_infant` | 29일~만 6개월 | WHO 완전모유수유 권장기간(0~6개월) 종료 시점 |
| `late_infant` | 만 6개월~12개월 | WHO 보완식(이유식) 도입 권장 시점(생후 6개월부터) |
| `toddler` | 만 12개월~24개월 | WHO 모유수유 지속 권장 종료 시점(만 2세) + 독립보행 발달이정표 구간 |
| `early_child` | 만 24개월~36개월 | WHO·UNICEF Nurturing Care Framework "0~3세" 초기아동발달 구간 종료점 |
| `preschool` | 만 36개월~60개월 | WHO·UNICEF "Under-five"(만 5세 미만) 통계 구간 종료점 |
| `school_age` | 만 60개월~96개월(만 8세) | 서비스 타겟 상한(만 8세 미만) 반영 |

**보정연령(Corrected Age):** WHO 기준 임신 37주 미만 출생은 조산아로 분류하며, 생후 24개월까지는 실제 출생일이 아니라 **출산예정일 기준 보정연령**으로 stage를 계산한다(AAP 권고와 동일). 임신 단계부터 앱을 사용한 유저는 저장된 `dueDate`와 실제 `birthDate`를 비교해 자동 판정하고, 출산 후 가입한 유저는 온보딩에서 "예정일보다 일찍 태어났나요?" 토글로 확인한다.

**보조 알림 트리거(세그먼트 버킷과는 별개):** 국내 영유아 국가건강검진 일정(14~35일/4~6개월/9~12개월/18~24개월/30~36개월/42~48개월/54~60개월/66~71개월)은 stage보다 촘촘해 세그먼트 버킷으로는 부적합하지만, "이번 달 검진 시기예요" 알림 트리거로는 유효하다. Phase 2 후보.

---

## 2. 데이터 모델

### 2.1 `children/{childId}` — 확장 필드

기존 필드(`userId, name, gender, birthDate, type, pregnancyWeek, dueDate, planningPeriod, feedingType, ageMonth, stage, categoryTags, weight, height, concerns[], careEnvironment[]`)에 추가:

| 필드 | 타입 | 비고 |
|---|---|---|
| `feedingType` | string | **기존 스키마에 있었으나 온보딩 UI가 없어 항상 'unknown'이었음 — 이번에 UI 추가 필요** (`breast`\|`formula`\|`mixed`\|`weaned`\|`unknown`) |
| `isPremature` | boolean | WHO 기준(37주 미만) |
| `correctedBirthDate` | Timestamp\|null | 조산 시 보정연령 계산용, 생후 24개월까지만 유효 |
| `allergyTags` | string[] | 다중선택 태그. `milk`\|`egg`\|`peanut_treenut`\|`wheat_gluten`\|`soy`\|`shellfish`\|`atopic_skin`\|`other`\|`none`(다른 태그와 상호배제) |
| `allergyOtherNote` | string | `allergyTags`에 `other`(기타) 포함 시에만 노출되는 자유 서술형 텍스트(예: "복숭아"). **매칭 가중치엔 미사용** — 목록에 없는 항목을 유저가 유실 없이 기록할 수 있게 하는 참고용 필드. `other` 미선택 시 항상 빈 문자열 |
| `isMultiple` | boolean | 다태아 여부 (동일/근접 생년월일 자녀 2명 이상 등록 시 자동 감지 가능) |
| `birthOrder` | number | **파생 필드** — 같은 userId 아래 자녀를 생년월일순 정렬해 자동 계산. 별도 질문 없음. 매칭 가중치엔 미사용, 메타데이터 전용 (§4 참조) |

### 2.2 `users/{userId}` — 추가 필드

| 필드 | 타입 | 비고 |
|---|---|---|
| `region` | string | 광역 단위 (수도권/충청/영남/호남/강원제주 등). 가족 단위 속성이므로 아이가 아니라 유저 문서에 저장. 세그먼트 매칭 + **향후 "우리 동네" 커뮤니티 필터**로 재사용 |
| `interestAreas` | string[] | 향후 서비스 확장(여행/교육/금융 등) 대비 의향 신호. §5.1 참조 |

### 2.3 상품 관련 액션 문서 — `childIds` 귀속 모델

`user_saved_products`, `reviews`, `reward_claims`, `posts`(상품 태그 시) 전체에 다음 필드를 추가한다.

```
childIds: string[]   // [] = 특정 아이 아님(범용템, 매칭 계산 제외)
                      // [id] = 단일 아이
                      // [id1, id2, ...] = 형제 공용템 (다중선택)
```

자녀 1명 유저는 자동 태깅으로 완전 무마찰. 자녀 2명 이상만 저장/리뷰/구매인증 시점에 "이 상품은 누구를 위한 건가요?"(다중선택 + "특정 아이 아님") 1회 선택. 조회/클릭 같은 저관여 로그(`user_product_actions`)는 강제 태깅하지 않는다 — 마찰 대비 실익이 낮음.

### 2.4 이벤트 시점 스냅샷 원칙

커뮤니티 게시글의 `authorBabyMonthAtCreation` 패턴(작성 시점 아이 개월수를 고정 스냅샷)을 상품 등록/관심상품저장/구매인증/리뷰 전체로 확장한다.

- 스냅샷 필드(`stage, gender, feedingType, careEnvironment, region, allergyTags, childIds` 등)는 **액션 생성 시점에 한 번만 기록**하고, 이후 게시글/리뷰 수정 시에도 절대 재계산하지 않는다 (`updatePost()`는 title/content/category/imageUrls만 갱신).
- **리뷰의 스냅샷 기준 시점은 "작성일"이 아니라 "구매인증 승인일"(`reward_claims.approvedAt`)** — 리뷰를 몇 달 뒤에 써도 실제 그 상품을 쓴 시점의 아이 정보가 정확하기 때문. 미인증 리뷰는 작성 시점 스냅샷을 최선의 근사치로 사용.
- 리뷰 작성 기한은 강제로 막지 않는다 (콘텐츠 볼륨 손실 대비 실익 낮음).

---

## 3. 리뷰/게시글 신뢰도 체계

- 콘텐츠 타입(기존 유지): `posts.category` = 질문/육아꿀템/특가제보/자유. 상품 태그는 **카테고리 무관하게 모든 게시글에 부착 가능**(이미 구현됨, `taggedProductId`는 category와 독립).
- **`verifiedPurchase: boolean`** — "이 유저가 이 태그된 상품에 대해 승인된 `reward_claims`를 보유하는가"로 자동 판정하는 **공통 유틸 함수**로 통일한다. 기존엔 `reviews`와 `posts`가 이 로직을 따로(사실상 `posts`는 아예 없이) 처리하고 있었음 — 이번에 하나로 통합.
- 노출 우선순위: 클린리뷰(구매인증) > 일반 리뷰/게시글. "인증 후기만 보기" 토글은 이 필드 하나로 양쪽 컬렉션 다 필터링 가능해야 함.

---

## 4. 유사도 매칭 — 연령 조건부 가중치

전 연령 고정 가중치는 부적절하다(예: 만 5세 아이에게 "수유방식" 가중치는 무의미). 축 개수도 최소화해 유저가 직관적으로 공감할 수 있게 한다.

| 축 | 가중치 | 활성 조건 |
|---|---|---|
| 연령대(stage) | 50% | 항상 — 압도적 1순위 |
| 양육환경 | 15% | 항상(단, 온보딩 입력 자체는 **선택**) — 미입력 시 해당 유저는 이 축에서 제외하고 나머지 활성 축에 비례 재분배(§6) |
| 성별 | 15% | 항상 |
| 지역 | 10% | 항상 |
| 알레르기/특이사항 | 5% | 항상 |
| 수유방식 | 5% | `newborn`~`toddler`(생후 0~24개월)만 활성. `early_child`(24개월) 진입 즉시 0으로 나머지 축에 비례 재분배 |

**형제서열(`birthOrder`)은 매칭 가중치에서 제외** — "또래 유사도" 신호가 아니라 §2.3의 데이터 오염 방지(정확한 귀속)를 위한 메타데이터로만 사용한다. `isMultiple`도 동일 — 니치 축이라 초기 코호트를 과도하게 쪼갤 위험.

**수유방식 축이 "감쇠"가 아니라 "구조적 제외"인 이유:** §5.4의 가중치 감쇠(양육환경 등)는 "데이터가 오래돼서 정확도가 떨어지는" 문제다 — 값 자체는 여전히 축으로서 유의미하지만 재확인이 안 됐을 뿐이다. 수유방식은 다르다. 생후 24개월(WHO 모유수유 지속 권장 종료 시점, §1)을 넘기면 사실상 모든 아이가 `weaned`(이유식완료/일반식)로 수렴해 응답이 균일해진다 — 즉 데이터가 "낡는" 게 아니라 **그 시점부터 값 자체가 변별력을 잃는다**(모두가 같은 답을 하는 축은 매칭에 아무 정보도 더해주지 못함). 그래서 시간에 따라 서서히 낮추는 게 아니라, `early_child` 진입 시점에 **즉시 0으로 하드 제외**하고 나머지 활성 축에 비례 재분배한다. 반대로 `pregnancy`/`planning` 상태는 애초에 아이가 태어나지 않아 물어볼 대상 자체가 없으므로(§6 온보딩 매트릭스) 이 축은 처음부터 비활성이다.

가중치 수치는 **가설값**이며 WHO 근거가 아니라 상품적 판단이다. 정답을 지금 확정하려 하기보다, 세그먼트별 CTR/체류시간 같은 실사용 데이터로 A/B 튜닝하는 것을 전제로 한다.

**알레르기 다중선택 입력 방식 (구현 완료, `OnboardingScreen.js`):** 알레르기는 한 아이가 여러 개를 동시에 가질 수 있으므로 단일선택이 아니라 **칩 다중선택**으로 받는다 (`milk`/`egg`/`peanut_treenut`/`wheat_gluten`/`soy`/`shellfish`/`atopic_skin`/`other`/`none`, `none`은 다른 태그와 상호배제). 매칭 태그(§4 MVP 구현의 `array-contains-any`)로 쓰기엔 사전 정의된 목록이 있어야 하므로, 목록에 없는 항목("복숭아 알레르기" 등)을 위해 `other`(기타) 선택 시에만 자유 서술형 `allergyOtherNote` 텍스트필드가 나타난다. 이 자유텍스트는 **매칭 가중치엔 사용하지 않는다** — 정형화되지 않은 문자열을 가중치 계산에 넣으면 오히려 §4 서두에서 경계한 "축 개수/노이즈" 문제를 자유텍스트로 재도입하는 셈이기 때문이다. 대신 (1) 유저가 정보를 유실 없이 남길 수 있게 하고, (2) 향후 특정 키워드가 반복 관찰되면 정식 태그로 승격하는 판단 근거로 쓴다.

**MVP 구현**: 각 아이/액션에 태그 배열(`['stage:toddler','gender:female','care:working_mom','region:capital','allergy:dairy']`)을 생성 → Firestore `array-contains-any`로 후보군 조회 → 겹치는 태그에 가중치를 곱해 앱/Cloud Function에서 후처리 정렬. 진짜 벡터/임베딩 매칭은 데이터가 쌓인 뒤 Phase 2(BigQuery 익스포트 또는 벡터DB)로 검토 — 지금 도입은 오버엔지니어링.

---

## 5. 미래 서비스 확장 대비 원칙 (여행/교육/금융 등)

핵심 원칙: 확장 대비는 **"지금 다 물어보기"가 아니라 "나중에 싸게 물어볼 수 있는 구조"**로 한다. 목적이 불명확한 민감정보(소득구간, 상세 건강기록, 여권정보, 소속기관명)를 "나중에 쓸 수도 있으니" 지금 수집하는 건 하지 않는다 — 개인정보 최소수집 원칙 위반이자 초기 신뢰도·전환율 리스크다.

### 5.1 관심 확장 영역 (`interestAreas`)

온보딩에 가벼운 다중선택 질문을 추가한다: "앞으로 어떤 정보를 더 받아보고 싶으세요?" — 상세 데이터가 아니라 **의향 신호만** 수집한다.

초기 태그 후보 (스키마 고정 아님, 배열이라 자유롭게 추가/삭제 가능): `travel`(여행) · `education`(교육) · `finance`(자녀 금융/저축) · `home_service`(돌봄·홈케어 등 생활서비스) · `health`(건강/의료) · `resale`(중고거래/나눔)

해당 서비스가 실제 론칭되고, 그 태그를 가진 유저가 그 기능에 처음 진입하는 시점에 그때 필요한 상세정보(여권유무, 학습관심사 등)를 그 자리에서 물어보는 **점진적 프로파일링(progressive profiling)** 방식을 취한다.

### 5.2 마케팅 동의 — 별도 인프라 없이 `interestAreas` 재사용

채널별 동의 체크박스를 미리 설계하지 않는다. 향후 특정 서비스 알림 발송 시 "일반 마케팅 동의 + 해당 `interestAreas` 태그 보유"를 기준으로 삼는다 — 신규 동의 UI·신규 인프라 불필요.

### 5.3 자녀 수는 물어보지 않고 파생한다

"자녀가 몇 명이세요"를 정적 질문으로 받으면 그 자체가 §5.4의 "낡는 데이터" 문제를 새로 만든다. 대신:
- 자녀 수 = `children` 컬렉션에서 해당 `userId`로 실시간 count (항상 정확, 재수집 불필요).
- 둘째 이상 등록 유도는 예측 질문이 아니라 **감지 가능한 이벤트 기반 넛지**로 처리한다: 등록된 아이가 `pregnancy` 상태인데 `dueDate`가 지났으면 → "출산하셨나요? 정보를 업데이트해주세요" 알림. 그 외 상황은 예측하지 않고, 마이페이지의 "+ 아이 추가" 상시 노출로 충분하다.

### 5.4 시간이 지나면 낡는 필드 — 팝업 재확인 대신 가중치 감쇠

필드 성격별로 다른 정책을 적용한다. **재확인 팝업으로 유저를 인터럽트하지 않는다.**

| 필드 유형 | 정책 |
|---|---|
| 거의 안 바뀜 (알레르기 등) | 감쇠 없음 |
| 행동 데이터로 더 정확히 갱신 가능 (지역) | 배송지 입력 등 실제 구매 행동에서 조용히 갱신 — 질문 자체가 불필요해짐 |
| 주기적으로 바뀜 (양육환경) | 6~12개월 경과 시 매칭 가중치 서서히 하락 |
| 발달단계와 연동됨 (향후 어린이집 재원여부 등 추가 시) | 시간이 아니라 **stage 전환 이벤트 발생 시 즉시 초기화** — 달력 기준보다 더 정확한 트리거 |

### 5.5 지금은 원칙적으로 수집하지 않는 것

소득구간, 상세 건강/의료기록, 여권정보, 자녀 소속기관 실명(어린이집/학교), 정확한 도로명주소. 사유: (1) 목적 불명확한 민감정보 수집은 개인정보보호법·앱스토어 심사 리스크 (2) 초기 앱 신뢰도에 마이너스 (3) 해당 서비스가 실제 론칭되는 시점에 명확한 목적과 함께 물어보는 게 전환율·법적 안정성 모두 낫다.

### 5.6 임신→출산 전환 감지 — "출산하셨나요?" 배너 (구현 완료)

§5.3에서 정의한 이벤트 기반 넛지의 실제 구현. `HomeScreen.js`에 `BirthCheckBanner`로 구현.

- **트리거**: 현재 선택된 아이가 `type === 'pregnancy'`이고 `dueDate`가 현재 시각보다 과거일 때. 예정일 자체가 대략치라("정확하지 않아도 괜찮아요" 카피 참조) 별도 유예기간 없이 예정일 당일부터 노출하되, 스누즈로 유저가 직접 미루도록 한다.
- **스누즈**: "아직이에요" 선택 시 `AsyncStorage`에 아이별로 14일 뒤 타임스탬프 저장, 그동안 배너 숨김. 영구 dismiss는 없음 — 임신은 결국 대부분 출산으로 이어지므로 계속 재노출.
- **확인**: "정보 업데이트" 선택 시 `ChildAddScreen`을 edit 모드로 열되 `birthDate: null`로 넘겨 신규 입력을 유도. `ChildAddScreen`엔 이미 "임신 중 → 아이"로 타입을 전환하면 "출산 후 아이 정보로 전환돼요. 저장 후 되돌릴 수 없어요" 안내가 있어 별도 확인 화면을 새로 만들지 않고 재사용했다.
- **보정연령과의 연결**: 이 흐름에서 유저가 실제 생년월일을 입력하면, 이미 저장되어 있던 `dueDate`와 비교해 조산 여부·보정연령을 **자동으로 판정할 수 있는 데이터가 이미 갖춰진다** (§1 참조). 이 자동 판정 로직 자체는 아직 미구현 — §8 TODO 참조.
- **미구현 부분**: `ChildAddScreen.js`는 아직 `allergyTags`/`feedingType`/`isPremature`를 입력받는 필드가 없다 (온보딩에만 추가됨). 출산 확인 시점에 이 필드들을 다시 못 채우고 넘어가게 된다 — §8 TODO.

---

## 6. 온보딩 질문 개정안

기존 유지: 임신/출산/계획 여부, 이름·성별·생년월일(또는 출산예정일), 키/몸무게(선택), 양육환경, 고민 카테고리(최대 3개, "없음"과 상호배제).

**신규 추가 (구현 완료, `OnboardingScreen.js`):**
1. 조산 여부 (출산 후 유형일 때만, 생년월일 입력 직후, 선택) — WHO 기준 보정연령 자동 판정용
2. 수유 방식 (모유/분유/혼합/이유식완료, **자녀있음 상태에만**, 필수) — 스키마엔 있었는데 UI가 없던 것을 이번에 채움
3. 지역 (광역 단위) — 유저 프로필 레벨. 이미 있었지만 저장이 안 되던 버그를 이번 세션에서 같이 고침
4. 알레르기/특이사항 (다중선택, "없음"과 상호배제, **자녀있음 상태에만**, 필수) — 최초엔 전체 상태 공통으로 넣었다가, "존재하지 않는 아이의 알레르기는 답할 수 없다"는 지적으로 자녀있음 전용으로 재수정. "기타" 선택 시 목록에 없는 알레르기를 유실 없이 남길 수 있도록 자유텍스트(`allergyOtherNote`) 입력란이 추가로 노출됨(매칭 가중치엔 미반영, §4 참조)
5. **관심 확장 영역** (다중선택, §5.1) — 3번째 스텝 관심사 다음에 배치, 스킵 가능, 전체 상태 공통 (부모/가구 단위 정보라 아이 존재 여부와 무관)

**육아환경 재설계 — 상태별 라벨 분기(저장값은 동일):** 최초엔 "육아환경" 질문을 자녀있음에만 노출했다가(→임신중/계획중 축 공백 버그) 전체 상태 공통으로 확장했는데, 그러자 "워킹맘"처럼 현재형 단어가 임신중/계획중 유저에게 어색하다는 지적이 있었다. 최종안: **저장되는 값(`워킹맘`/`전업맘`/`조부모 도움`/`기타`)은 상태 무관 동일하게 유지**하되, 화면에 보이는 **라벨만 상태별로 분기**한다 (자녀있음: "워킹맘" 그대로 / 임신중·계획중: "복직 예정" 등 미래형으로 표시 + "아직 미정" 옵션 추가). 이러면 매칭 로직은 상태를 신경 쓸 필요 없이 동일한 값으로 비교할 수 있고, 오히려 "복직 예정인 예비맘"과 "이미 워킹맘인 선배" 간 매칭이 자연스럽게 성립한다.

**육아환경 입력 자체는 필수 → 선택으로 전환(확정, 구현 완료):** "복직 예정" 같은 값은 복직 이후에도 유저가 마이페이지에서 굳이 고치지 않을 확률이 높고("워킹맘이냐 아니냐"가 제품 만족도를 크게 가르는 축도 아님), 재확인을 강제할 만큼 매칭 기여도가 크지 않다고 판단해 §5.4 가중치 감쇠 대상 축과 동일하게 취급한다. 온보딩에서 필수(`*`) 표기를 없애고 "선택"으로 변경, `step2Valid`도 `parentingEnv` 미입력을 막지 않도록 수정했다. 미입력 유저는 §4 매칭 계산에서 이 축만 제외하고 나머지 활성 축에 비례 재분배된다.

**명시적으로 추가하지 않은 것:** 자녀 수를 묻는 정적 질문(§5.3), 채널별 마케팅 동의 체크박스(§5.2), 다태아 여부(니치 축이라 §4에서 매칭 가중치 제외 결정과 함께 보류 — 필요해지면 §2.1 `isMultiple` 자동감지 로직으로 추가).

**제거한 것 (기존에 있었으나 정리):**
- "반려동물과 함께하시나요?" — 답변을 물어놓고 어디에도 저장이 안 되던 질문. 매칭 축에도 없어 삭제.
- "첫째 아이인가요?" (임신중 전용) — `birthOrder`는 §2.1에서 자동 파생하기로 이미 결정했는데 별도로 또 물어보고 있었음 + 미저장. 중복이라 삭제.
- "임신 준비를 위해 영양제를 챙기시나요?" (계획중 전용) — 미저장이었고, 이미 3단계 관심사 칩에 "임신 준비/영양제"가 동일 내용으로 있어 중복. 삭제.

**상태별(자녀있음/임신중/계획중) 질문 분화 — 실제 확인 결과와 수정 내역:**

| 항목 | 자녀있음 | 임신중 | 계획중 | 비고 |
|---|---|---|---|---|
| 이름/성별/생년월일 또는 태명/출산예정일/계획시기 | ✅ | ✅ | ✅ | 기존부터 상태별로 분화되어 있었음 |
| 육아환경(양육환경, 매칭 15%) | 선택 (현재형 라벨) | 선택 (미래형 라벨, 저장값 동일) | 선택 (미래형 라벨, 저장값 동일) | 원래 자녀있음에만 물어봐서 공백이었던 버그 수정 + 상태별 라벨 분기로 어색함도 해결. 재확인 안 해도 큰 문제 없는 축이라 필수→선택 전환 |
| 수유방식(매칭 5%, 조건부) | ✅ | 해당없음 | 해당없음 | 아직 아이가 없는 상태라 물어볼 수 없음 — 정상 |
| 알레르기(매칭 5%) | ✅ | 해당없음 | 해당없음 | 아직 아이가 없는 상태라 물어볼 수 없음 — 최초엔 전체 공통으로 잘못 설계했다가 수정 |
| 지역(매칭 10%) | ✅ | ✅ | ✅ | 기존부터 공통, 저장 버그만 수정 |
| 성별(매칭 15%) | ✅ | 해당없음(출생 전) | 해당없음 | 구조상 당연 |
| 관심 카테고리(concerns) | 상태별 다른 목록 | 상태별 다른 목록 | 상태별 다른 목록 | 기존부터 분화되어 있었음, 유지 |
| 관심 확장 영역(interestAreas) | ✅ | ✅ | ✅ | 신규 추가, 전체 상태 공통 |

**온보딩이 아닌 별도 마이크로 인터랙션**: "이 상품은 누구를 위한 건가요" — 자녀 2명 이상 유저가 저장/리뷰/구매인증 시점에만 노출. (§2.3, 아직 미구현)

---

## 7. Firestore 인덱스

기존 13개는 `firestore.indexes.json`에 캡쳐 완료(2026-08-12, 이전엔 콘솔에서만 만들어져 버전관리 밖에 있었음).

**신규 스키마 구현 시 추가 필요 (TODO — 필드가 실제로 쌓이기 전엔 선제 배포하지 않음):**
- `user_saved_products`: `childIds`(array-contains) + `userId` + `createdAt`
- `reviews` / `posts`: `verifiedPurchase` + `region` (지역 기반 클린리뷰 노출용)
- `posts`: `region` + `createdAt` (향후 "우리 동네" 커뮤니티 필터)

---

## 8. 미결정 / TODO

- [x] ~~온보딩 화면 UI 반영: 조산여부/수유방식/알레르기/관심확장영역~~ — 구현 완료 (§6)
- [x] ~~상태별(자녀있음/임신중/계획중) 질문 분화 감사~~ — 완료, 육아환경 누락 버그 발견·수정 (§6)
- [x] ~~임신→출산 전환 감지 배너~~ — `BirthCheckBanner` 구현 완료 (§5.6)
- [ ] 위 가중치 초기값에 대한 최종 컨펌
- [ ] `childIds` 다중선택 UI 문구/플로우 확정 (저장/리뷰/구매인증 3곳 동일 컴포넌트로 재사용 예정)
- [ ] `verifiedPurchase` 공통 판정 유틸 함수 설계 및 구현
- [ ] `children`/`users` 스키마 확장 필드 마이그레이션 (기존 유저 대상 기본값 처리)
- [ ] §5.4 가중치 감쇠 로직 구현 위치 결정 (Cloud Function 배치 vs 조회 시점 계산)
- [ ] `interestAreas` 태그 목록 최종 확정 (§5.1은 초안)
- [ ] **`ChildAddScreen.js`에 `allergyTags`/`feedingType`/`isPremature` 입력 필드 추가** — 온보딩엔 추가됐지만 이 화면(마이페이지 수정, 출산확인 배너 진입점)엔 아직 없어서, 출산 확인 시점에 이 필드들을 못 채움 (§5.6)
- [ ] 조산 여부(`isPremature`) → 보정연령(`correctedBirthDate`) 자동 계산 로직 — 현재는 boolean만 저장, `stage` 계산에 아직 반영 안 됨
- [ ] 다태아 여부(`isMultiple`) 자동 감지 로직 (생년월일 근접한 자녀 2명 이상 등록 시)

---

## 9. 세션 로그 (이 문서가 언제 왜 바뀌었는지)

- 2026-08-12: 최초 작성 (WHO 단계 프레임워크, childIds 모델, 가중치 매칭, 온보딩 개정 v1)
- 2026-08-12: 온보딩 실제 라이브 화면 확인 — `InitialOnboardingScreen.js`는 미연결 고아 코드였고 `OnboardingScreen.js`가 실제 화면임을 확인. `OnboardingScreen.js`가 수집한 데이터를 Firestore에 전혀 저장하지 않던 버그 수정 (`createChild`/`updateSelectedChild`/`region` 저장 연결)
- 2026-08-12: §5 미래 서비스 확장 대비 원칙 추가 (관심영역 태그, 마케팅동의 재사용, 자녀수 파생, 가중치 감쇠 정책, 원칙적 수집 배제 항목)
- 2026-08-18: 온보딩 실제 구현 — 알레르기/수유방식/조산여부/관심확장영역 UI 추가, 반려동물/첫째여부/영양제 질문 제거(미저장·중복이었음), 육아환경을 전체 상태 공통으로 확장(임신중·계획중에서 빠져있던 버그 수정). `childrenRepository.js`에 `allergyTags`/`isPremature` 필드 추가. `HomeScreen.js`에 임신→출산 전환 감지 배너(`BirthCheckBanner`) 구현 — `ChildAddScreen.js`의 기존 타입 전환 UI를 재사용.
- 2026-08-18(2차): 수유방식 축을 "만 3세 이전"이라는 모호한 표현에서 `newborn`~`toddler`(생후 0~24개월) 활성/`early_child` 이후 즉시 하드 제외로 명확화, §5.4 가중치 감쇠(값은 유효하나 재확인 안 됨)와 다른 개념(그 시점부터 값 자체가 변별력을 잃음)임을 §4에 명시. 알레르기 "기타" 선택 시 자유텍스트(`allergyOtherNote`) 입력 UI 구현 완료(`OnboardingScreen.js`) + `childrenRepository.js` 화이트리스트 반영 — 매칭 가중치엔 미사용, 유실 방지·향후 정식 태그 승격 판단용. §0.1 신설 — 4대 전략 축(뾰족한 사용이유/유저데이터 명확성/정밀마케팅/커뮤니티MAU)과 문서 각 절의 연결을 명시해, 향후 축 추가/배제 판단 기준으로 삼음.
- 2026-08-18(3차): 육아환경 입력을 필수 → 선택으로 전환 확정. "복직 예정" 등은 복직 후에도 재입력 안 할 확률이 높고 제품 만족도를 크게 가르는 축도 아니라는 판단(§5.4 감쇠 대상과 동일 취급). `OnboardingScreen.js`의 `step2Valid`에서 `parentingEnv.length > 0` 조건 제거, UI 필수(`*`) 표기를 "선택"으로 변경. §4/§6 표와 §8 반영.
