- [2026-03-25] UI/UX Refactor: removed duplicate headers, scoped FAB to MyPage, updated Benefits CTA, and completely redesigned MyPage layout (Hwahae/Polsent style).
- [2026-03-25] Implemented real Coupang Best Categories API in RankingScreen (mapped 1011, 1012, 1014) with detailed guidance banner, and overhauled CommunityScreen to Naver Cafe style.
- [2026-03-25] Security & API: Configured .env for Coupang API keys, fixed getBestCategoryProducts HMAC logic, and deployed with real production keys.
- [2026-03-25] Bugfix: Analyzed Firebase logs, found root cause was Cloud Run IAM 401 (not Coupang HMAC). Added invoker:'public' to getBestCategoryProducts onCall options so Firebase CLI sets allUsers as invoker. Added full error body logging for Coupang HTTP errors and rCode failures. Redeployed getBestCategoryProducts.
- [2026-03-26] Bugfix: Firebase logs showed Coupang returning HTTP 401 with body {"code":"ERROR","message":"Invalid signature."}. Root cause: getBestCategoryProducts was signing only the bare path in HMAC but the Coupang API requires the query string to be included in the signed message (same as searchProducts which already works). Fixed by building pathWithQuery = path + '?' + queryStr and passing that to buildPartnersAuth instead of hmacPath. Redeployed getBestCategoryProducts.
- [2026-03-26] Bugfix: Exhaustively tested all HMAC path variants (bare vs query, with/without /v1/) against Coupang API directly. Found: Coupang verifies signature against the EXACT request URL. Any query param (?limit=N, ?subId=...) in the URL causes 401 unless IDENTICAL in signed path — but even with matching query, it fails. Only variant with bare path in BOTH signature AND request URL returns 200. Fixed by removing all query params from both signedPath and requestUrl. Redeployed getBestCategoryProducts.
