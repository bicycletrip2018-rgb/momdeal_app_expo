# MomDeal Change History

<!-- Append one line per session under the relevant date. -->

- [2026-03-23] Added `searchProducts`, `getProductDetail`, `generateDeeplink` Firebase Functions (HMAC-SHA256 signed, keys via `functions/.env`); created `src/services/coupangApiService.js` as the secure bridge (`searchCoupangProducts`, `getCoupangProductDetail`); Coupang API keys confined to `functions/` — zero key exposure in `src/`.
- [2026-03-23] Implemented Home screen Hero/Medium/List UI in `Tab1_ProductList.js`; wired `searchCoupangProducts('기저귀', 20)` as fallback data when no child is registered; applied 5 trust-based recommendation copy strings (cyclic by index) as reason fallback for all product cards.
- [2026-03-23] Optimized ProductDetail CTA: CTA always enabled (fallback to Coupang search URL when no affiliateUrl); standardized action log to `actionType: 'product_purchase_click'` with `productGroupId` in both `recordProductAction` and `recordAbTestAction`; updated `productActionService.js` to write optional `productGroupId` field.
- [2026-03-23] Enhanced ProductDetail CTA UI: Made button sticky (fixed footer outside ScrollView, `useSafeAreaInsets` for bottom safe area); updated default CTA text to '쿠팡에서 최저가 확인하기'; added trust-inducing micro-copy nudge '비슷한 상품 중에서도 선택이 높은 제품이에요' above the button.
- [2026-03-23] Built RankingScreen UI with 3 Coupang category tabs (출산/유아동 1011, 식품/분유 1012, 생활용품 1014); added `getBestCategoryProducts` Firebase Function (HMAC-signed GET bestcategories API, max 50 items); added `fetchBestCategoryProducts` to `coupangApiService.js`; default category 1011 loads on mount; tapping a ranked product navigates to ProductDetail.
- [2026-03-23] Built Community UI (후기/질문/자유/지역/전체) with 구매 인증 badge; implemented hyper-personalized context-to-commerce in PostDetailScreen: keyword extraction from post text, Coupang candidate fetch via searchCoupangProducts, peer behavior scoring (same-stage parent action counts from user_product_actions), top-3 horizontal card carousel with dynamic trust copy ('비슷한 개월 수 부모님들이 많이 찾은 제품이에요' or fallback), post_product_click logging.
- [2026-03-23] Built BenefitsScreen UI focusing on non-monetary gamification: Profile & Level section (nickname + Lv.N 배지 derived from streak/missions, 🔥 연속 접속 streak); Daily Mission section with step indicator, 3 task rows with individual '보러가기' navigation buttons (홈/랭킹/커뮤니티); locked Secret 시크릿 템 section with placeholder cards that unlock when all missions complete; Badge grid (탐험가/랭킹러/소통왕/오늘의 챔피언).
- [2026-03-23] Updated MyPageScreen with Firestore nickname (getOrCreateNickname), horizontal scroll for '최근 본 상품' with image thumbnails, empty state '최근 본 상품이 없습니다. 랭킹을 확인해보세요!' linking to 랭킹 tab, '다시 보러가기' CTA button on purchase-click history cards, always-visible both sections with graceful empty states.
- [2026-03-24] Built `registerProductFromUrl` CF with multi-market routing architecture (switch/case, Coupang mocked, naver/11st stubbed for future); Firestore doc written with `market`, `originalId`, `productGroupId` (`market_id` composite key) + initial offer snapshot; added '가격 추적 상품 등록' TextInput+등록 button section at top of MyPageScreen with ActivityIndicator and success/error Alerts.
- [2026-03-24] Finalized registerProductFromUrl pipeline: replaced mock with real Partners API → v4 JSON API → fallback chain in `fetchProductDetailsByMarket`; added `createdAt` on new docs only (deduplication via `existing.get()` check); `isNew` flag returned to client; MyPage error handler maps `functions/invalid-argument` → URL-specific copy, `functions/not-found` → product-not-found copy, other → generic retry message.
- [2026-03-24] Built Community Context-to-Commerce: replaced `searchCoupangProducts` fallback in PostDetailScreen with Firestore-based matching using `generateProductTags` (categoryTags + stageTags derived from post text); `fetchContextualProducts` queries active products, client-filters by tag overlap + child stage, scores by peer action counts, Coupang fallback when 0 Firestore matches; added `post_view` action log on mount; carousel renamed to "이 글과 연관된 또래 추천 아이템"; skeleton placeholder cards shown while products load async; trust copy updated to "성장이 비슷한 또래 부모들이 80% 이상 선택했어요"; `post_product_click` logs `productGroupId` for both Firestore and Coupang items.
- [2026-03-24] Enhanced Home Screen personalization: split UI into Section A '아이와 비슷한 친구들이 보고 있는 상품' (±3mo floating window horizontal carousel, `product_click`+`productGroupId` logging) and Section B '지금 맘딜에서 가장 인기 있는 상품' (general popular vertical list); added `getFloatingWindowProducts(child, windowMonths=3)` export to recommendationService — scores stageMatch*0.7 + peerScore(±3mo)*0.3, returns top-10 stage-matched items; graceful empty state when peer data is sparse.
- [2026-03-24] Implemented intelligent product tagging: rewrote `productTagService.js` with two-pass engine (STAGE_KW_MAP additive stage detection → CATEGORY_RULES first-match category + default stages → supplemental KEYWORD_DICT merge); all 7 stages aligned with childStageUtils.js; added server-side CommonJS mirror `tagProduct()` in functions/index.js; registerProductFromUrl now writes `stageTags[]`, `categoryTags[]`, `problemTags[]` to Firestore on every registration.
- [2026-03-24] Completed Benefits Screen gamification: created `missionService.js` (MISSION_DEFS, checkMissionStatus, allMissionsComplete); updated `useAuthSync.js` to write `streakCount`+`lastActiveDate` once per calendar day (falls back to legacy `streak` field); refactored `BenefitsScreen.js` to source missions from MISSION_DEFS, call `checkMissionStatus` in both useEffect and useFocusEffect (removing duplicate inline query logic), read `streakCount??streak` (read-only — no longer writes streak), show '✅ 완료됨' disabled button when mission complete; added `ranking_visit` action log on mount in `RankingScreen.js` to enable the 랭킹 mission.
- [2026-03-24] Built Price Tracking Scheduler: added `scheduledPriceUpdate` pubsub CF (every 6h) in `functions/index.js` — queries active products in batches of 5 with 2s delay, fetches fresh price via `fetchProductDetailsByMarket`, writes to `product_price_history` (feeds bar chart), updates `currentPrice`+`updatedAt` on product doc when price changes, adds snapshot to `offers` sub-collection, writes `lastPriceDrop`+`lastPriceDropPct` on product doc for >5% drops (cleared on price rise), logs `price_drop_alert` action; expanded offers fetch in `ProductDetail.js` to last 10 snapshots, added "지금 사기 좋은 가격인가요?" insight badge (✅/❌/⚠️ per guidance) and recent price change list; added 📉 가격 하락 badges to saved items (uses `priceDrop` from `getSavedProductsWithPriceSignals`) and recent viewed products (uses `lastPriceDrop` from product doc) in `MyPageScreen.js`.
- [2026-03-24] Implemented Tiered Price Tracking and Peer-Action CTA: refactored `scheduledPriceUpdate` to tiered logic (Tier A = `product_click/view/purchase_click` in last 48h → every 6h; Tier B = others → only if `priceLastUpdatedAt` null or >24h old), batch size 10 + 1s delay; writes `priceLastUpdatedAt` instead of `updatedAt`; logs `price_drop_event` (replaces `price_drop_alert`); added `formatPriceTimestamp` helper to `ProductDetail.js` showing "방금 전" / "N시간 전" / "YYYY-MM-DD" based on `priceLastUpdatedAt`; added "🔥 최근 24시간 내 또래 엄마들이 N번 구매 확인" peer-purchase badge (from `product_purchase_click` actions in last 24h) and "📉 일주일 전보다 ₩N 저렴해졌어요" week-ago price badge (derived from `offerSnapshots`); updated `Tab1_ProductList.js` product card drop badge to 📉 emoji.
- [2026-03-24] Initiated Admin Analytics: created `adminAnalyticsService.js` with `getDailyStats` (today's views/CTR/notification-opens via single-field createdAt query), `getTopConvertedProducts` (purchase-click rank), `getStageDistribution` (children collection by stage), `getRecentPurchaseIntentList` (recent purchase-click events with product names, createdAt-desc query to avoid composite index), `getNotificationStats` (open-rate proxy), and `computeAndWriteSelectionRates` (batch-writes `selectionRate` 0–100 to product docs based on last-30d active users); created `AdminDashboardScreen.js` with stat cards (CTR, hottest stage, alert open-rate), top-10 converted products rank list, stage distribution bar chart, recent revenue-signal list, and admin utility "지표 새로고침" button; added `AdminDashboard` to `MyPageStack` in `App.js`; added `isAdmin` state to `MyPageScreen.js` (reads `users/{uid}.role === 'admin'`) with conditional `📊 어드민 대시보드` entry button; added `selectionRate` social proof label to `ProductDetail.js` info block ("전체 맘딜 유저 중 N%가 이 제품을 선택했어요") using product doc field set by the admin utility.
- [2026-03-24] Built Notification System: added `onPriceDropNotify` Firestore onCreate trigger in `functions/index.js` — fires on `price_drop_event` actions, queries `user_saved_products` + `user_product_actions` (2+ product_views in 7 days) to build eligible user set, writes to `notifications/{userId}/user_notifications` sub-collection (no composite index needed — date filter applied client-side); created `NotificationScreen.js` with FlatList of notifications, unread badge, 📉/💬/🔔 type icons, tap-to-mark-read + `notification_open` action logging, deeplink navigation to ProductDetail or PostDetail; wired screen into `PriceStack` in `App.js` with `🔔` header button on home screen; refined purchase-intent micro-copy in `ProductDetail.js`: "N번 구매 확인" → "N명의 또래 엄마들이 쿠팡에서 확인 중".
- [2026-03-24] Built Profile Dashboard & Concern Matching: added `CONCERN_OPTIONS` multi-select ('피부/기저귀','수면/재우기','수유/이유식','발달/놀이','안전/외출') to `ChildAddScreen.js`; `childrenRepository.normalizeChildPayload` now writes `concerns[]` (defaults to `[]` for backwards-compat); added `concernMatch` signal to `toRecommendationItem` (+0.2 when child concerns intersect `problemTags`/`categoryTags`) and to `getFloatingWindowProducts` (additive concernBonus); `MyPageScreen` profile header shows concern chips (purple, hidden when empty), new 3-stat activity row ("추적 중인 상품" / "작성한 후기" / "구매 클릭") — review count fetched via single-field `where('userId','==',uid)` query added to existing parallel `Promise.all`.
- [2026-03-24] Built Purchase Success Review system: updated `reviewService.submitReview` to accept `purchasePrice` + auto-compute `isBestDeal` (within 5% of historical min via `getLowestPrice`); `ReviewWriteScreen` now forwards `currentPrice` route param; `ProductDetail` passes `currentPrice` to ReviewWrite navigation, renders "🎉 최근 7일간 N명이 최저가 구매 성공" banner (client-filtered from already-fetched reviews), and "💸 핫딜 구매 성공 (₩N에 구매)" badge on individual review cards; added `onReviewCreate` Firestore trigger to `functions/index.js` (server-side reviewStats recompute + `review_written` action log, single-field query); `PostDetailScreen` shows `💸 핫딜 ₩N 하락` badge on related product cards when post category is `review` and product has `lastPriceDrop > 0`.
- [2026-03-24] Built Social Sharing System: created `sharingService.js` with `generateShareLink` (bridge URL → `handleShareLink` CF) and `shareProduct` (native Share API + `product_share` action log with `priceVerdict`); added 공유 + bookmark dual header buttons to `ProductDetail.js` (share message is BEST_TIME-aware: "🔥 역대급 가격" vs generic); added `handleShareLink` HTTPS onRequest CF to `functions/index.js` — resolves affiliate URL from offers sub-collection → product doc → fallback Coupang URL, redirects 302, logs `share_link_click`.
- [2026-03-24] Built Price Intelligence UI: added `computePriceInsight(snapshots)` (pure) and `getPriceInsight(productGroupId)` (async) to `recommendationService.js` — reads `products/{id}/offers` sub-collection, computes minPrice/avgPrice/currentPricePosition (0–100%), returns `BEST_TIME` (≤5% above min), `GOOD_TIME` (≤avg), or `WAIT` (>avg) verdict; added "판단 가이드" verdict badge to `ProductDetail.js` (✅/⚠️/❌ with human-readable copy); CTA nudge text goes dynamic — BEST_TIME shows "지난 30일 중 가장 저렴한 가격이에요!"; verdict logged as `priceVerdict` in `product_view` action for A/B funnel analysis.
- [2026-03-25] Rebranded app to SaveRoo (세이브루): updated `app.json` (name → "세이브루", slug → "saveroo", bundleIdentifier → "com.saveroo.app", android package → "com.saveroo.app"); updated `package.json` name → "saveroo"; replaced all 8 UI occurrences of "맘딜" across `sharingService.js`, `userRepository.js`, `BenefitsScreen.js`, `Tab1_ProductList.js`, `RankingScreen.js`, `ProductDetail.js` (×2), `SearchScreen.js` with "세이브루".
- [2026-03-25] Finalized Onboarding & Branding: applied 'Warm & Expert' UX copywriting to all 3 OnboardingScreen steps (new headings/subs and Step 3 CTA "맞춤 추천 시작하기 🎉"); updated `app.json` name → "맘딜 (Momdeal)", slug → "momdeal"; confirmed all asset paths (`icon.png`, `splash-icon.png`, `adaptive-icon.png`) exist under `./assets/`; splash `backgroundColor` confirmed `#ffffff`.
- [2026-03-25] Built Welcome Onboarding Flow: created `src/screens/OnboardingScreen.js` — 3-step wizard (Step 1: name+gender toggle, Step 2: child/pregnancy type + YYYY-MM-DD birthdate with live parse feedback, Step 3: 2×3 concern multi-select grid with check mark); progress bar with fill animation; pink CTA buttons; "나중에 하기" skip on all steps; on completion calls `createChild` then `updateSelectedChild` before transitioning; updated `App.js` to check children count via `onAuthStateChanged` → `getChildrenByUserId` and route new users (0 children) through `RootStack` with `OnboardingScreen`; `MainTabs` extracted to its own function; `GlobalFab` only rendered after onboarding completes; splash spinner shown during auth/children check.
- [2026-03-25] Implemented Polsent-inspired UX: added `PriceLineGraph` component (connected-dot sparkline with rotation-based segments, labeled min/max/current points, stats row) replacing text-based "최근 가격 변동" table in `ProductDetail.js`; added categorized wishlist filter chips (`전체` / `🚨 지금이 최저가` / `⏳ 가격 하락 대기`, filtering by `currentPrice <= lowestPrice * 1.05`) to `MyPageScreen.js` saved products section; created `src/components/GlobalFab.js` (pink FAB anchored above tab bar → Modal bottom sheet with market logos, Coupang URL input + `registerProductFromUrl` CF call, Kurly/Naver marked 준비 중); created `src/screens/RewardClaimScreen.js` (explains manual order-number flow, product selector from tracked items, order number input, saves to `reward_claims` Firestore with `status: 'pending'`); wired GlobalFab into `App.js` as absolute overlay and added `RewardClaim` route to `MyPageStack`.
- [2026-03-25] Implemented Out-of-Stock (OOS) Graceful Degradation: added backend OOS detection in price scheduler (`scheduledPriceUpdate` writes `isOutOfStock` boolean to product doc on every price check, handles no-price OOS case without skipping); filtered OOS items from all recommendation feeds (`getRecommendedProducts`, `getFloatingWindowProducts`, `fetchContextualProducts`); updated `ProductDetail.js` CTA to disable + gray button + "현재 품절된 상품입니다" text, hides "판단 가이드" verdict badge and urgency/social proof chips when OOS; added dim image overlay + "품절" dark badge in `SearchScreen.js` product cards.
- [2026-03-24] Built Search-to-DB Engine: created `src/services/searchService.js` (hybrid Firestore local + Coupang fallback when <5 local results, `source: 'local'|'coupang'` tagging, `logSearchQuery` fire-and-forget); created `src/screens/SearchScreen.js` (TextInput search bar with cancel, AsyncStorage recent searches with per-item delete + clear-all, FlatList result cards with source badge and price, empty state, auto-focus); wired 🔍 header button in `PriceList` (App.js) alongside existing 🔔; background auto-registration via `registerProductFromUrl` CF fires on Coupang result press without blocking navigation.
